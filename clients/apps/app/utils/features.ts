// Accounts (login + account screens) are deactivated for the public
// launch. The auth code is kept but no sign-in entry point is shown, and the
// login / authenticated routes redirect home. Flip to `true` to re-enable.
export const ACCOUNTS_ENABLED = false
