/** Uppercase micro-kicker style shared by the source cards' timestamps and meta
 * labels — a one-off letterspaced micro type, not a Text variant. */
export const KICKER_STYLE = {
  fontSize: 10,
  textTransform: 'uppercase',
  letterSpacing: 1.2,
} as const
