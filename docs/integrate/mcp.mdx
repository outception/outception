---
title: Outception over Model Context Protocol (MCP)
description: Extend the capabilities of your AI agents with Outception's MCP Server
sidebarTitle: Model Context Protocol (MCP)
---

Supercharge your AI agents with Outception as a Model Context Protocol (MCP) server.

## What is MCP?

MCP is a protocol for integrating tools with AI agents. It can greatly enhance the capabilities of your AI agents by providing them with real-time data and context.

Outception offers a remote MCP server that you can connect to from most AI clients.

## How does it work?

You need a MCP-capable agent environment to use Outception over MCP. A few of them are Claude Desktop and Cursor.

## Connecting to Outception MCP

Outception provides two MCP servers:

- **Production**: `https://mcp.outception.com/mcp/outception-mcp` - Connect to your live Outception organization
- **Sandbox**: `https://mcp.outception.com/mcp/outception-sandbox` - Connect to the Outception sandbox environment for testing

When you can specify a MCP URL, use one of the URLs above depending on your environment.

If you have to specify a command, use:

```json
{
  "mcpServers": {
    "Outception": {
      "command": "npx",
      "args": ["mcp-remote", "https://mcp.outception.com/mcp/outception-mcp"]
    }
  }
}
```

For sandbox:

```json
{
  "mcpServers": {
    "Outception Sandbox": {
      "command": "npx",
      "args": ["mcp-remote", "https://mcp.outception.com/mcp/outception-sandbox"]
    }
  }
}
```

### Cursor

In `.cursor/mcp.json`, add:

```json
{
  "mcpServers": {
    "Outception": {
      "url": "https://mcp.outception.com/mcp/outception-mcp"
    }
  }
}
```

For sandbox:

```json
{
  "mcpServers": {
    "Outception Sandbox": {
      "url": "https://mcp.outception.com/mcp/outception-sandbox"
    }
  }
}
```

### Windsurf

In `mcp_config.json`, add:

```json
{
  "mcpServers": {
    "Outception": {
      "command": "npx",
      "args": ["mcp-remote", "https://mcp.outception.com/mcp/outception-mcp"]
    }
  }
}
```

For sandbox:

```json
{
  "mcpServers": {
    "Outception Sandbox": {
      "command": "npx",
      "args": ["mcp-remote", "https://mcp.outception.com/mcp/outception-sandbox"]
    }
  }
}
```

### Codex

Add the following to your `~/.codex/config.toml`:

```toml
[features]
rmcp_client = true

[mcp_servers.outception]
type = "http"
url = "https://mcp.outception.com/mcp/outception-mcp"
```

Then run:

```sh
codex mcp login outception
```

For sandbox:

```toml
[features]
rmcp_client = true

[mcp_servers.outception_sandbox]
type = "http"
url = "https://mcp.outception.com/mcp/outception-sandbox"
```

Then run:

```sh
codex mcp login outception_sandbox
```

### Claude Code

Run the following command:

```
claude mcp add --transport http "Outception" "https://mcp.outception.com/mcp/outception-mcp"
```

For sandbox:

```
claude mcp add --transport http "Outception-Sandbox" "https://mcp.outception.com/mcp/outception-sandbox"
```

### OpenCode

In `opencode.jsonc`, add:

```jsonc
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "Outception": {
      "type": "remote",
      "url": "https://mcp.outception.com/mcp/outception-mcp"
    }
  }
}
```

For sandbox:

```jsonc
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "Outception Sandbox": {
      "type": "remote",
      "url": "https://mcp.outception.com/mcp/outception-sandbox"
    }
  }
}
```

### ChatGPT

MCP is only available for paid users in beta on ChatGPT web, by enabling Developer Mode.

Once Developer Mode is enabled, go to _Settings_ → _Connectors_ and add the MCP server using `https://mcp.outception.com/mcp/outception-mcp`.

For sandbox, use `https://mcp.outception.com/mcp/outception-sandbox` instead.

### Claude Desktop

Go to _Settings_ → _Connectors_ and click _Add custom connector_.

Name it "Outception" and add `https://mcp.outception.com/mcp/outception-mcp` as the server URL.

For sandbox, use `https://mcp.outception.com/mcp/outception-sandbox` as the server URL instead.

Save, and click _Connect_ to connect to your Outception organization.
